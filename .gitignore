_site
.bundle
.sass-cache
.jekyll-cache
.jekyll-metadata
.DS_store
.ruby-version
.tweet-cache
Gemfile.lock
assets/libs/
node_modules/
vendor
.idea

# Large/copyrighted source PDF for the deeplearning wiki — kept locally, not published
# (the wiki pages are generated from the .md files; the PDF is unreferenced & excluded from the build)
deeplearning/*.pdf